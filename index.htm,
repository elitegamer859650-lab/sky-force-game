<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no">
    <title>SKY FORCE - Paradox Studio</title>
    <style>
        body { margin: 0; overflow: hidden; background: #000; font-family: 'Courier New', Courier, monospace; }
        canvas { display: block; }

        /* Game UI Overlay [cite: 2025-12-23] */
        #game-ui { position: absolute; top: 0; left: 0; width: 100%; height: 100%; pointer-events: none; }
        
        /* Numeric UID & Level Badge [cite: 2025-12-23] */
        .header { position: absolute; top: 20px; left: 20px; background: rgba(0,0,0,0.8); padding: 10px; border: 1px solid #ff0000; pointer-events: auto; }
        .uid-text { color: #00ffff; font-size: 20px; font-weight: bold; }
        .level-badge { color: gold; font-size: 14px; }

        /* God Mode Power Button [cite: 2025-12-18] */
        #god-mode-btn { 
            position: absolute; bottom: 30px; right: 30px; 
            width: 80px; height: 80px; background: red; 
            border-radius: 50%; border: 3px solid white; 
            color: white; font-weight: bold; cursor: pointer; 
            pointer-events: auto; box-shadow: 0 0 20px red;
        }

        /* Virtual Joystick for Mobile/Chromebook [cite: 2025-12-18] */
        #joystick { position: absolute; bottom: 50px; left: 50px; width: 100px; height: 100px; background: rgba(255,255,255,0.1); border-radius: 50%; pointer-events: auto; }
    </style>
</head>
<body>

    <div id="game-ui">
        <div class="header" onclick="alert('Player: Paradox Hacker \nStatus: Level 17 Hardcore')">
            <div class="uid-text">UID: 87654321</div>
            <div class="level-badge">LEVEL 17 (HARDCORE)</div>
        </div>

        <button id="god-mode-btn">GOD<br>MODE</button>
        <div id="joystick"></div>
    </div>

    <script type="importmap">
        {
            "imports": {
                "three": "https://unpkg.com/three@0.150.1/build/three.module.js",
                "three/addons/": "https://unpkg.com/three@0.150.1/examples/jsm/"
            }
        }
    </script>

    <script type="module">
        import * as THREE from 'three';
        import { GLTFLoader } from 'three/addons/loaders/GLTFLoader.js';

        // 1. Scene & Environment
        const scene = new THREE.Scene();
        scene.background = new THREE.Color(0x0a0a0a); // Dark MMA Theme

        const camera = new THREE.PerspectiveCamera(75, window.innerWidth / window.innerHeight, 0.1, 1000);
        camera.position.set(0, 2, 5);

        const renderer = new THREE.WebGLRenderer({ antialias: true });
        renderer.setSize(window.innerWidth, window.innerHeight);
        renderer.outputEncoding = THREE.sRGBEncoding; // High Quality Textures [cite: 2025-12-18]
        document.body.appendChild(renderer.domElement);

        // 2. Lights (Safari Glow)
        const light = new THREE.AmbientLight(0xffffff, 1);
        scene.add(light);

        // 3. Loading Models from Google Drive / Assets Folder
        const loader = new GLTFLoader();
        // Dhayeen! Pehla model load ho raha hai [cite: 2025-12-18]
        loader.load('assets/safari_bundle.glb', (gltf) => {
            const player = gltf.scene;
            scene.add(player);
            console.log("Safari Bundle Loaded Successfully!");
        }, undefined, (e) => console.error("Asset Not Found!", e));

        // 4. God Mode Animation Logic [cite: 2025-12-18]
        let godPower = false;
        document.getElementById('god-mode-btn').onclick = () => {
            godPower = !godPower;
            alert(godPower ? "God Mode Activated! 😈" : "God Mode Off");
        };

        function animate() {
            requestAnimationFrame(animate);
            renderer.render(scene, camera);
        }
        animate();
    </script>
</body>
</html>
